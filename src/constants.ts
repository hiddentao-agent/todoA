/** Maximum allowed length for a task text in characters. */
export const MAX_TASK_LENGTH = 1000;

/** Character count threshold at which a warning indicator is shown. */
export const CHAR_WARN_THRESHOLD = 900;
